const sumAll = function(one, two) {
    if(!isNaN(one) ){

    }
    let sum = 0;
    for (let i = one; i < two; i++) {
        sum = sum + i;
        
    }
    return sum + two

};

// Do not edit below this line
module.exports = sumAll;
